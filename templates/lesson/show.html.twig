{% extends 'base.html.twig' %}

{% block title %}Lesson{% endblock %}

{% block body %}
    <h1>{{ lesson.nameLesson }}</h1>
    <p>Курс:</p>
    <a href="{{ path('app_course_show', {'id': lesson.idcourse.id }) }}">{{ lesson.idCourse.nameCourse }}</a>
    <br><br>
    <a href="{{ path('app_lesson_edit', {'id': lesson.id}) }}">Редактировать</a>
    {{ include('lesson/_delete_form.html.twig') }}

    <p>Содержимое урока:</p>
    <p>{{ lesson.contentLesson }}</p>
{% endblock %}
