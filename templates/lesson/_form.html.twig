{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary" >
        <strong>{{ button_label|default('Сохранить') }}</strong>
    </button>
{{ form_end(form) }}
