{{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success"><strong>{{ button_label|default('Сохранить') }}</strong></button>
{{ form_end(form) }}
