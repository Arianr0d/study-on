{% extends 'base.html.twig' %}

{% block title %}{{ course.nameCourse }}{% endblock %}

{% block body %}

    <h1>{{ course.nameCourse }}</h1>
    <p>{{ course.descriptionCourse }}</p>

    <h1>Уроки</h1>
    {% for lesson in course.lessons %}
        <p>{{ loop.index }}. <a href="{{ path('app_lesson_show', {'id': lesson.id}) }}">{{ lesson.nameLesson }}</a></p>
    {% endfor %}
    <br>

    <a href="{{ path('app_course_index') }}">К списку курсов</a>
    <a href="{{ path('app_course_edit', {'id': course.id}) }}">Редактировать курс</a>
    {#<a href="{{ path('app_lesson_new', {'course': course}) }}">Добавить урок</a>#}

    {{ include('course/_delete_form.html.twig') }}
{% endblock %}
